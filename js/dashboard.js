async function renderDashboard(){setTitulo('Controle de Caixa','Resumo financeiro da escola');const [entradas,saidas,saldoInicialRaw]=await Promise.all([buscar('entradas'),buscar('saidas'),buscarConfig('saldoInicial')]);const saldoInicial=Number(saldoInicialRaw||0);const totalEntradas=entradas.reduce((s,i)=>s+Number(i.valor),0);const totalSaidas=saidas.reduce((s,i)=>s+Number(i.valor),0);const saldo=saldoInicial+totalEntradas-totalSaidas;const ultimos=[...entradas.map(e=>({...e,tipo:'Entrada'})),...saidas.map(s=>({...s,tipo:'Saída'}))].sort((a,b)=>b.data.localeCompare(a.data)).slice(0,10);document.getElementById('app').innerHTML=`<div class="cards"><div class="card verde"><span>Saldo</span><strong>${moeda(saldo)}</strong></div><div class="card azul"><span>Entradas</span><strong>${moeda(totalEntradas)}</strong></div><div class="card laranja"><span>Saídas</span><strong>${moeda(totalSaidas)}</strong></div><div class="card vermelho"><span>Saldo Inicial</span><strong>${moeda(saldoInicial)}</strong></div></div><div class="painel"><h2>Últimos lançamentos</h2>${ultimos.length?`<table class="table"><thead><tr><th>Data</th><th>Tipo</th><th>Descrição</th><th>Valor</th></tr></thead><tbody>${ultimos.map(i=>`<tr><td>${dataBR(i.data)}</td><td>${i.tipo}</td><td>${escapeHtml(i.descricao)}</td><td>${moeda(i.valor)}</td></tr>`).join('')}</tbody></table>`:'<p class="msg">Nenhum lançamento cadastrado.</p>'}</div>`;}
